cmake_minimum_required(VERSION 3.25)
project(hyprflip VERSION 0.2.0 LANGUAGES CXX)
set(CMAKE_CXX_STANDARD 26)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_EXPORT_COMPILE_COMMANDS ON)

find_package(PkgConfig REQUIRED)
pkg_check_modules(HYPRLAND REQUIRED IMPORTED_TARGET hyprland)
# Hyprland main still reports 0.56.0, so match the exact commit instead.
set(HYPRFLIP_HYPRLAND_COMMIT "e368c13c27a42a173b9e08fa0bf413f9f7073187" CACHE STRING "Hyprland commit this plugin targets")
find_file(HYPRLAND_VERSION_H hyprland/src/version.h PATHS ${HYPRLAND_INCLUDE_DIRS} NO_DEFAULT_PATH NO_CACHE)
if(NOT HYPRLAND_VERSION_H)
  message(FATAL_ERROR "Cannot find hyprland/src/version.h to verify the Hyprland commit")
endif()
file(STRINGS "${HYPRLAND_VERSION_H}" HYPRLAND_COMMIT_LINE REGEX "GIT_COMMIT_HASH")
if(NOT HYPRLAND_COMMIT_LINE MATCHES "\"${HYPRFLIP_HYPRLAND_COMMIT}\"")
  message(FATAL_ERROR "Hyprflip targets Hyprland ${HYPRFLIP_HYPRLAND_COMMIT}; found: ${HYPRLAND_COMMIT_LINE}")
endif()
pkg_search_module(LUA REQUIRED IMPORTED_TARGET lua5.5 lua55 lua-5.5 lua-55)
if(LUA_VERSION VERSION_LESS 5.5 OR NOT LUA_VERSION VERSION_LESS 5.6)
  message(FATAL_ERROR "Hyprland 0.56.2 requires Lua 5.5")
endif()
pkg_check_modules(GLES REQUIRED IMPORTED_TARGET glesv2)
add_library(hyprflip MODULE src/Plugin.cpp src/Controller.cpp src/FlipTransformer.cpp src/FloatingCards.cpp)
set_target_properties(hyprflip PROPERTIES PREFIX "")
target_link_libraries(hyprflip PRIVATE PkgConfig::HYPRLAND PkgConfig::LUA PkgConfig::GLES ${CMAKE_DL_LIBS})
target_compile_options(hyprflip PRIVATE -Wall -Wextra -Wpedantic -Wno-unused-parameter -fno-gnu-unique)
target_compile_definitions(hyprflip PRIVATE WLR_USE_UNSTABLE HYPRFLIP_VERSION="${PROJECT_VERSION}")
include(CTest)
if(BUILD_TESTING)
  add_library(motion_probe MODULE tests/motion_probe.cpp)
  set_target_properties(motion_probe PROPERTIES PREFIX "")
  target_link_libraries(motion_probe PRIVATE PkgConfig::HYPRLAND)
  target_compile_options(motion_probe PRIVATE -Wall -Wextra -Wpedantic -Wno-unused-parameter -fno-gnu-unique)
  target_compile_definitions(motion_probe PRIVATE WLR_USE_UNSTABLE)
  add_executable(core_test tests/core_test.cpp)
  target_include_directories(core_test PRIVATE src)
  target_compile_options(core_test PRIVATE -Wall -Wextra -Wpedantic)
  add_test(NAME core COMMAND core_test)
endif()
install(TARGETS hyprflip LIBRARY DESTINATION lib/hyprland/plugins)
